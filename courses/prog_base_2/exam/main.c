#include <stdio.h>
#include <stdlib.h>

int main()
{
int n=100;
char str[100] = "asfafaf";
puts(str);
 return 0;
}
bool check(int n=100, char str[n])
{
    int i;
    for(i=0;i<n;i++){
        if(str[i]!='('|| str[i]!=')'||str[i]!=' ')
            return 0;
    }
    for(i=0;i<n;i++)
    {
        if()
    }

}

